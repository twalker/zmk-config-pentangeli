if SHIELD_PENTANGELI_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "pentangeli"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_PENTANGELI_LEFT || SHIELD_PENTANGELI_RIGHT

config ZMK_SPLIT
    default y

endif
